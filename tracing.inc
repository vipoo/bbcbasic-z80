

#ifdef TRACING

	EXTERN	PRTSTRD
	EXTERN	PRT_HLHLC, PRT_DEDEB
	EXTERN	PRTHEXWORDHL, PRTHEXWORDIX, PRTHEXWORDDE
	EXTERN	PRTHEX, PRTHEX_D, PRTHEX_C, PRTHEX_B
	EXTERN	PRTDEC_HL, PRTDEC_DE

#define DEBUG(S) 	CALL	PRTSTRD 		!
			defm	S 			!
			defb	'$'

#define TRACE_DEC_HL	CALL	PRTDEC_HL
#define TRACE_DEC_DE	CALL	PRTDEC_DE

#define TRACE_HL	CALL	PRTHEXWORDHL
#define TRACE_IX	CALL	PRTHEXWORDIX
#define TRACE_DE	CALL	PRTHEXWORDDE

#define TRACE_D		CALL	PRTHEX_D
#define TRACE_C		CALL	PRTHEX_C
#define TRACE_B		CALL	PRTHEX_B
#define TRACE_A		CALL	PRTHEX

#define TRACE_HLHLC	CALL	PRT_HLHLC
#define TRACE_DEDEB	CALL	PRT_DEDEB

#endif
